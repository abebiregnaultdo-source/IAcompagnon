# Commande de déploiement

Avant de déployer, lis le fichier `.claude/project-notes.md` pour les instructions.

## Checklist déploiement :

1. **Git push** → Remote `origin` = `abebiregnaultdo-source/IAcompagnon`
   - Si auth demandée : utiliser compte `abebiregnaultdo-source`

2. **Vercel** → Déployer manuellement : `cd frontend && npx vercel --prod`
   - Projet : `chris-projects-8e78a4a1/ia-compagnon`

3. **Render** → Automatique après git push

Exécute les étapes de déploiement demandées par l'utilisateur.
