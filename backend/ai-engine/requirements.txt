fastapi==0.115.2
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
jinja2==3.1.4
openai>=1.0.0
anthropic>=0.34.0

# Emotion analysis (lexical-based, lightweight ~5MB)
nrclex>=3.0.0

# Note: torch/transformers/sentence-transformers/chromadb removed
# The code has fallbacks that work without these heavy libraries:
# - emotion_detector.py falls back to NRCLex
# - vector_rag.py falls back to keyword search

